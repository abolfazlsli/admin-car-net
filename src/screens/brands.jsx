import { useEffect, useState } from "react";
import { createBrand, getBrands, updateBrand, deleteBrand } from "../network/api";

const Brands = () => {
    // const [brands, setBrands] = useState([]);
    
    
    return (
        <div>
            <h1>Brands</h1>
        </div>
    )
}   

export default Brands;
